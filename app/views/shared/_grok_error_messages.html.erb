<% if @grok.errors.any? %>
  <div id="grok_errors">
    <div class="alert alert-error">
      The form contains <%= pluralize(grok.errors.count, "error") %>.
    </div>
    <ul>
    <% grok.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>