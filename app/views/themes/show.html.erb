<title><%= full_title(yield(:title)) %></title>
<p id="notice"><%= notice %></p>
<h3><%= @theme.keyword %></h3>
 <div class="row">
          <div id = "grok_div"></div>
          	<div class="col-sm-6 col-md-6 col-lg-6" id="groks">
          <%= render @theme.groks %>
          	</div>
 </div>
<ul>
<%= will_paginate @groks %>
</ul>
<%= link_to 'Back', themes_path %><br>
 <% if current_user.try(:admin?) %>
<%= link_to 'Add New', new_theme_path %>
<% end %>