<%#- if theme.changeable_by?(current_user) -%>
<%= link_to('<< Back to Grok', {:action => 'new'}, :class => 'back-link') %>

<%= form_for(@theme, :url => {:action => :update, :id => @theme.id}) do |f| %>
<div class="field">
    <%= f.label :keyword %><br>
    <%= f.text_area :keyword %>
  </div>
  <%#<div class="field">
    <%= f.label :book_id %><%#<br>
    <%#= f.number_field :book_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%#= render(:partial => 'form', :locals => {:f => f}) %>  
<%# end %>


<%= link_to 'Index', themes_path %>