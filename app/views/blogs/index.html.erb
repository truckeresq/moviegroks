<title><%= full_title(yield(:title)) %></title>
<h1>Check out this week's new movies</h1>
<br>
 <% @blogs.each_slice(3) do |blogs| %>
    <div class="row">
    <% blogs.each do |blog| %>
    <div id="blog_div"></div>
     <div class="col-md-4 col-lg-4" id="blog">
       
        <%= render blog %>
<br>
          
        </div> 
      <% end %>
  </div>
  <% end %>

<ul>
 <% will_paginate %>
</ul>
<% if current_user.try(:admin?) %>
<%= link_to 'Add New', new_blog_path %>
<% end %>

 
